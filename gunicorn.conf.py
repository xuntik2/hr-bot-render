import os

# На каком порту будет работать бот (Render сам подставит сюда PORT=10000)
bind = f"0.0.0.0:{os.getenv('PORT', '10000')}"

# ВАЖНАЯ СТРОКА: Указываем Gunicorn использовать потоковый воркер (встроенный в него)
worker_class = "gthread"

# Количество процессов (воркеров). Для бесплатного тарифа Render ставим 1
workers = 1

# Количество потоков внутри одного воркера. 4 — оптимально для небольшого бота
threads = 4

# Если запрос обрабатывается дольше 120 секунд — отменить его (защита от "зависаний")
timeout = 120

# Уровень детализации логов (info — только важные сообщения)
loglevel = os.getenv("GUNICORN_LOG_LEVEL", "info")

# Куда писать логи (в консоль Render)
accesslog = "-"
errorlog = "-"

# Просто сообщение для логов при запуске
print(f"🚀 Gunicorn запущен с конфигурацией:")
print(f"   Воркер: {worker_class}")
print(f"   Воркеров: {workers}")
print(f"   Потоков: {threads}")
print(f"   Порт: {bind}")
print(f"   Таймаут: {timeout} сек")
